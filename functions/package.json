{
  "name": "wowshopping-auth-functions",
  "private": true,
  "engines": {"node": "20"},
  "main": "index.js",
  "scripts": {"serve": "firebase emulators:start --only functions,firestore,hosting", "deploy": "firebase deploy --only functions,firestore,hosting"},
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "resend": "^4.0.0"
  }
}
